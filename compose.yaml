services:
  web:
    build:
      context: .
      dockerfile: Dockerfile.dev
    ports:
      - '${PORT:-5173}:5173'
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      VITE_API_URL: '${VITE_API_URL:-http://localhost:9999}'
    env_file: .env
